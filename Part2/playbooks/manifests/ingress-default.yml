apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: app3-default
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: HostRegexp(`{host:.+}`)
      kind: Rule
      priority: 1
      services:
        - name: app3
          port: 80