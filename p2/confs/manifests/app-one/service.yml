apiVersion: v1
kind: Service
metadata:
  name: app-one
  namespace: p2
  labels:
    app: app1
spec:
  type: ClusterIP
  selector:
    app: app-one
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
