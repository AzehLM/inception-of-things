---
- name: Configure agent node and install K3s agent
  hosts: all
  become: true 
  gather_facts: false

  vars:
    node_ip: "192.168.56.111"
    flannel_iface: "eth1"
    # reference to control-panel (server node) so the agent can join the pod cluster
    # the URL have to match the --advertise-address used on the server configuration (6443 is the default API port)
    k3s_server_url: "https://192.168.56.110:6443"

  tasks:
    - name: Install python3
      ansible.builtin.raw: apk add --no-cache python3
      changed_when: false

    - name: Gather facts (now that python3 is available)
      ansible.builtin.gather_facts:

    - name: Disable SSH password authentication
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?\s*PasswordAuthentication\s+'
        line: 'PasswordAuthentication no'
      register: ssh_password_auth_changed

    - name: Disable SSH challenge-response/keyboard-interactive authentication
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?\s*KbdInteractiveAuthentication\s+'
        line: 'KbdInteractiveAuthentication no'
      register: ssh_kbdint_auth_changed

    - name: Restrict root login to key-based authentication only
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?\s*PermitRootLogin\s+'
        line: 'PermitRootLogin prohibit-password'
      register: ssh_root_login_changed

    - name: Restart sshd to apply SSH hardening
      ansible.builtin.service:
        name: sshd
        state: restarted
      when: >
        ssh_password_auth_changed.changed or
        ssh_kbdint_auth_changed.changed or
        ssh_root_login_changed.changed

    - name: Ensure cgroup mode is set to unified in rc.conf
      ansible.builtin.lineinfile:
        path: /etc/rc.conf
        regexp: '^#?\s*rc_cgroup_mode='
        line: 'rc_cgroup_mode="unified"'
        backup: true
      register: cgroup_mode_changed

    - name: Ensure cgroups OpenRC service is enabled at boot runlevel
      ansible.builtin.command:
        cmd: rc-update add cgroups boot
      register: cgroup_service_added
      changed_when: "'already' not in cgroup_service_added.stdout"
      failed_when:
        - cgroup_service_added.rc != 0
        - "'already' not in cgroup_service_added.stderr"

    - name: Reboot the VM so the unified cgroup2 mount takes effect
      ansible.builtin.reboot:
        reboot_timeout: 120
        connect_timeout: 5
        post_reboot_delay: 5
        msg: "Rebooting to apply cgroup2 unified mode"
      when: cgroup_mode_changed.changed or cgroup_service_added.changed

    - name: Verify cgroup2 unified controllers are available
      ansible.builtin.command:
        cmd: cat /sys/fs/cgroup/cgroup.controllers
      register: cgroup_controllers
      changed_when: false

    - name: Fail early if expected cgroup controllers are missing
      ansible.builtin.fail:
        msg: >
          Expected cgroup controllers not found. Got: "{{ cgroup_controllers.stdout }}".
          K3s/kubelet will not start correctly without cpu, memory, pids, io, cpuset.
      when: >
        not (cgroup_controllers.stdout is search('cpu') and
             cgroup_controllers.stdout is search('memory') and
             cgroup_controllers.stdout is search('pids') and
             cgroup_controllers.stdout is search('io') and
             cgroup_controllers.stdout is search('cpuset'))

    - name: Read node-token from the K3s server
      ansible.builtin.slurp: # slurp reads a file's content form a target (here the node-token of the server node)
        src: /var/lib/rancher/k3s/server/node-token
      register: node_token_raw
      delegate_to: guebersoS # this allow us to run the slurp on another host. Here, only our server node have access to this token so it needs to be rrun on ther other VM

    - name: Set the node token as a fact
      ansible.builtin.set_fact: # set_fact creates or overwrites a variable
        k3s_node_token: "{{ node_token_raw.content | b64decode | trim }}" # slurp encodes the value read in base64 so we need to decode it to store it properly

    - name: Check if K3s agent is already installed
      ansible.builtin.stat:
        path: /usr/local/bin/k3s
      register: k3s_binary

    - name: Download K3s install script
      ansible.builtin.get_url:
        url: https://get.k3s.io
        dest: /tmp/k3s-install.sh
        mode: "0755"
      when: not k3s_binary.stat.exists

    - name: Install and start K3s in agent (worker) mode
      ansible.builtin.command:
        cmd: > # same as the server but we install k3s in agent mode, using the k3s_node_token we got from the above tasks
          /tmp/k3s-install.sh agent 
          --node-ip={{ node_ip }}
          --flannel-iface={{ flannel_iface }}
          --server={{ k3s_server_url }}
          --token={{ k3s_node_token }}
      when: not k3s_binary.stat.exists

    - name: Wait for K3s agent service to report started
      ansible.builtin.command:
        cmd: rc-service k3s-agent status # using k3s-agent here, k3s is reserved for the server role
      register: k3s_status
      until: "'started' in k3s_status.stdout"
      retries: 12
      delay: 5
      changed_when: false
